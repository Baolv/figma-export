{
  "name": "figma-design-export-plugin",
  "version": "1.3.0",
  "description": "Figma plugin that exports full design data (tokens, layout, spacing, components) and real icon/image assets for Claude to implement UI 1:1.",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --target=es2017 --outfile=code.js",
    "watch": "esbuild src/code.ts --bundle --target=es2017 --outfile=code.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.23.0",
    "fflate": "^0.8.3",
    "typescript": "^5.5.0"
  }
}
